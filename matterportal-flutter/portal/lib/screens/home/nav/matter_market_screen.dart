import 'package:flutter/material.dart';

class MatterMarketScreen extends StatelessWidget {
  const MatterMarketScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Market'));
  }
}
